{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn example start",
    "start-elm": "yarn example start",
    "build": "yarn backstitch build",
    "postinstall": "yarn backstitch build",
    "backstitch": "yarn workspace backstitch",
    "example": "yarn workspace backstitch-example",
    "example-elm": "yarn workspace backstitch-example-elm",
    "compile": "tsc --noEmit"
  },
  "devDependencies": {
    "ts-node": "^8.0.1",
    "typescript": "^3.2.4"
  }
}
