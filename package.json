{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "elm": "elm=$PWD/node_modules/elm/bin/elm && cd packages/example-elm && $elm",
    "compile": "tsc --noEmit"
  },
  "devDependencies": {
    "ts-node": "^8.0.1",
    "typescript": "^3.2.4"
  }
}
