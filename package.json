{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "elm": "elm=$PWD/node_modules/elm/bin/elm && cd packages/example-elm && $elm",
    "compile": "lerna run compile"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "ts-node": "^8.0.1",
    "typescript": "^3.2.4"
  }
}
